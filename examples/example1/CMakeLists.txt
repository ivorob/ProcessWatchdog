CMAKE_MINIMUM_REQUIRED (VERSION 3.0)
PROJECT (example1)

SET (SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR})
SET (SOURCES
        ${SRC_DIR}/main.cpp
    )

ADD_EXECUTABLE (example1 ${SOURCES})
TARGET_LINK_LIBRARIES (example1 libProcessWatchdog)
